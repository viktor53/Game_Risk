<UserControl x:Class="Risk.Views.ConnectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Risk.Views"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
    <Grid.RowDefinitions>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="*"/>
      <ColumnDefinition Width="Auto"/>
      <ColumnDefinition Width="*"/>
    </Grid.ColumnDefinitions>
    <Border Background="{StaticResource MenuBg2}" CornerRadius="5" BorderThickness="3" BorderBrush="Black" Grid.Column="1" Grid.Row="1">
      <StackPanel>
        <StackPanel Orientation="Horizontal" Margin="30, 30, 30, 10">
          <Label Style="{StaticResource LabelUnivers}" Content="Nickname:" Margin="0,5,5,5"/>
          <TextBox Style="{StaticResource TextBoxUnivers}" Margin="5, 5, 0, 5" Width="200" Height="30" FontSize="15" FontWeight="Bold"/>
        </StackPanel>
        <Button Style="{StaticResource MenuButton}" Command="{Binding Connect_Click}" Content="Connect" Margin="30,10,30,30"/>
      </StackPanel>
    </Border>
  </Grid>
</UserControl>
